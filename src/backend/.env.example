# Application Configuration
# ------------------------
# Environment mode (development, staging, production)
NODE_ENV=development

# Server port number (1024-65535)
PORT=3000

# Base API URL (must be HTTPS in production)
API_BASE_URL=https://api.precheck.me

# Database Configuration
# ---------------------
# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=precheck_db
DB_USER=precheck_user
DB_PASSWORD=your_strong_password_here
DB_SSL=true

# Redis Configuration
# ------------------
# Redis connection settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here
REDIS_CLUSTER_MODE=false

# Authentication Configuration
# --------------------------
# JWT settings (secrets should be at least 32 chars)
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_here
JWT_EXPIRES_IN=24h

# Session configuration
SESSION_SECRET=your_very_long_and_secure_session_secret_here

# Multi-factor authentication
MFA_ENABLED=true

# AWS S3 Storage Configuration
# --------------------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
S3_BUCKET_NAME=precheck-documents

# Email Configuration
# -----------------
# SMTP settings for email delivery
SMTP_HOST=smtp.provider.com
SMTP_PORT=587
SMTP_USER=notifications@precheck.me
SMTP_PASSWORD=your_smtp_password_here

# Note: This is an example configuration file.
# For production deployment:
# 1. Never commit actual credentials to version control
# 2. Use strong, unique passwords for all sensitive values
# 3. Enable SSL/TLS for all external connections
# 4. Use secrets management service in production
# 5. Ensure all URLs use HTTPS in production
# 6. Enable MFA and cluster mode in production